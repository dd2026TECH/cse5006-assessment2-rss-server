# Local development. In Docker, compose overrides this with the service name:
#   postgresql://user:password@postgres:5432/mydb
# Not committed (.gitignore covers .env*) — see .env.example.
DATABASE_URL="postgresql://user:password@localhost:5432/mydb"
